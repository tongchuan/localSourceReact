const initState = {
  list: [],
  currentPage: 1,
  pageCount: 0,
  pageSize: 20,
  totalCount: 0,
}

export default function(state=initState,action){
  switch (action) {
    case 'expression':

      break;
    default:

  }
  return state
}
